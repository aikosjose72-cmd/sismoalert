{
  "name": "gnss-proxy",
  "version": "1.0.0",
  "description": "Proxy con CORS para consultar velocidades GNSS reales del Nevada Geodetic Laboratory (MIDAS) desde el navegador.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
